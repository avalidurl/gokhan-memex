# Draft posts

MDX files here are **not** part of the Astro `blog` collection and are **not** published on the site. To publish, move a file into `src/content/blog/` and set `draft: false` (or remove `draft`).
