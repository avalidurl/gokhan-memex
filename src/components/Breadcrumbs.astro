---
import { ChevronRight, Home } from 'lucide-astro'

export interface BreadcrumbItem {
  label: string
  href: string
}

interface Props {
  items: BreadcrumbItem[]
  currentPage: string
}

const { items, currentPage } = Astro.props
---

<nav aria-label="breadcrumb" class="mb-6 overflow-x-auto pb-2">
  <ol class="flex min-w-max items-center gap-2 whitespace-nowrap text-sm text-muted-foreground">
    <!-- Home -->
    <li>
      <a 
        href="/" 
        class="flex items-center hover:text-foreground transition-colors"
        aria-label="Home"
      >
        <Home class="h-4 w-4" />
      </a>
    </li>
    
    <!-- Breadcrumb items -->
    {items.map((item) => (
      <>
        <li class="flex-shrink-0">
          <ChevronRight class="h-4 w-4" />
        </li>
        <li class="flex-shrink-0">
          <a 
            href={item.href}
            class="hover:text-foreground transition-colors"
          >
            {item.label}
          </a>
        </li>
      </>
    ))}
    
    <!-- Current page -->
    {currentPage && (
      <>
        <li class="flex-shrink-0">
          <ChevronRight class="h-4 w-4" />
        </li>
        <li class="max-w-[12rem] truncate text-foreground font-medium sm:max-w-none" title={currentPage}>
          {currentPage}
        </li>
      </>
    )}
  </ol>
</nav>
